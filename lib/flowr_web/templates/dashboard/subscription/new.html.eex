<h1>New Subscription</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.dashboard_subscription_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.dashboard_subscription_path(@conn, :index) %></span>
